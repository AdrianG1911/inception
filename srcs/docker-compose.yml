version: '3.7'

services:
    nginx:
        build: ./requirements/nginx
        ports:
            - "443:443"
        networks:
            - inception
        restart: always

volumes:
    nginx_data:

networks:
    inception: